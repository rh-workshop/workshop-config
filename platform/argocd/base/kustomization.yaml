# Configuración del propio Argo CD: proyectos que acotan qué puede desplegarse.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-gitops

resources:
  - workshop-platform-appproject.yaml
  - argocd-gateway-api-clusterrole.yaml
