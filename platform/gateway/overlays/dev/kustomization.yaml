# Overlay del gateway compartido para el cluster del workshop.
# La base ya trae el nombre de dominio de este cluster; el overlay existe para
# que un cluster distinto solo tenga que sobrescribir el hostname aquí.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-gateway

resources:
  - ../../base
