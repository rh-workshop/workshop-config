# Overlay del entorno de pruebas.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: workshop-demo-test

resources:
  - ../../base
  - namespace.yaml
  - servicio-demo-httproute.yaml

images:
  - name: servicio-demo
    newName: image-registry.openshift-image-registry.svc:5000/workshop-demo-test/servicio-demo
    newTag: "1.0.0"

patches:
  - path: servicio-demo-configmap.yaml
    target:
      kind: ConfigMap
      name: servicio-demo-config
  # A diferencia de desarrollo, pruebas ejecuta dos réplicas: el mismo "qué"
  # de la base con un "cuánto" distinto, que es el papel de un overlay.
  - path: deployment-replicas.yaml
    target:
      kind: Deployment
      name: servicio-demo
