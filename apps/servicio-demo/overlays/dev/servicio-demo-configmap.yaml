# Valores de configuración propios del entorno de desarrollo.
apiVersion: v1
kind: ConfigMap
metadata:
  name: servicio-demo-config
data:
  APP_VERSION: "1.0.0"
  APP_ENTORNO: "dev"
  APP_MENSAJE: "Servicio de demostración — entorno de desarrollo"
